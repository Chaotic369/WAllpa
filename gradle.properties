# Required: the project uses AndroidX (Compose, Room, Media3, DataStore...).
# Without this, :app:checkDebugAarMetadata fails on AGP 8.x.
android.useAndroidX=true

# Give Gradle/KSP/Compose enough memory on the CI runner
org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8

kotlin.code.style=official